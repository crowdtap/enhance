// Generated by CoffeeScript 1.6.3
(function() {
  var _;

<<<<<<< HEAD
  _ = require('../vendor/lodash/dist/lodash');

  _.mixin({
    joinURIComponents: function() {
      var components, leadingSlashes, uri, _ref;
      leadingSlashes = ((_ref = arguments[0]) != null ? _ref.match(/^[\/]{1,2}/) : void 0) || '';
      components = _.map(arguments, function(str, i) {
        if (str != null) {
          return _.trim(str, '/').split(/\/(?!\/)/);
        }
      });
      return uri = leadingSlashes + _.flatten(_.compact(components)).join('/');
    },
    trim: function(string, characters) {
      var regexp;
      if (characters == null) {
        characters = '\\s';
      }
      regexp = new RegExp("^[" + characters + "]+|[" + characters + "]+$", 'g');
      return string.replace(regexp, '');
=======
  _ = require('../vendor/lodash');

  _.mixin({
    joinURIComponents: function() {
      var components;
      components = _.map(arguments, function(str) {
        return str != null ? str.split(/\/(?!\/)/) : void 0;
      });
      return _.flatten(_.compact(components)).join('/');
>>>>>>> compile coffee files manually
    }
  });

  module.exports = _;

}).call(this);
